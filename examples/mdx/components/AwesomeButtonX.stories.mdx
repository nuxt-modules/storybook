import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import AwesomeButton from '../components/AwesomeButton.vue';

<Meta title='AwesomeButton2' component={AwesomeButton} argTypes={{
  label: { control: 'text' },
  color: { control: 'color' },
}}  />

# This is an awesome button

Some **markdown** description, or whatever you want.

<Story name='basic button' args={{ label: 'hello', color: '#ff0' }}>{{
  template: '<AwesomeButton @click.native="clickme" :label="label" :color="color" />',
  props: {
    label: {
      default: 'Awesome button',
    },
    color: {
      default: '#777777'
    }
  },
  methods: {
    clickme () {
      action('button-click')()
    }
  }
}}</Story>

## Example

```javascript
  <AwesomeButton @click.native="clickme" :label="label" :color="color" />
```

## Props

<Props of={AwesomeButton} />